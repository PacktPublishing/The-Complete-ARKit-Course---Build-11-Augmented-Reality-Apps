Distance (Resources)
Distance Function used in lecture

    func distance(x: Float, y: Float, z: Float) -> Float {
    
    return (sqrtf(x*x + y*y + z*z))
    }
The Xcode file is in the resources folder

Make sure to clean the project before running it. 